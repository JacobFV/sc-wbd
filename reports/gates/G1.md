# G1 — COULD_NOT_RUN

**Claim.** Typed, source-native fusion is preferable to naive resampling.

**Falsified by (thesis).** No reproducible gain over single-modality or carefully tuned resampling baselines; increased overconfidence or negative transfer.

*thesis V6 · schema scwbd-schema/1.0.0 · bench scwbd-bench-report/1.0.0 · SC-WBD-001-beta · seed 0 · git 1a35a9a · 2026-08-06T19:02:45+00:00*

## Could not run

> This gate did **not** pass. It did not run. Nothing may be claimed on its basis.

## Sub-checks

| check | mandatory | status | detail |
|---|---|---|---|
| inputs[0] | yes | COULD_NOT_RUN | missing: typed fusion candidate (agent I / agent E) |
| inputs[1] | yes | COULD_NOT_RUN | missing: held-out train/test datasets (agent B source cards) |
| inputs[2] | yes | COULD_NOT_RUN | missing: baseline 'naive_resampling' (the thing the claim is against) |
| inputs[3] | yes | COULD_NOT_RUN | missing: at least one baseline named 'single_modality_*' |

## Blocking reasons

- inputs[0]: could not run — missing: typed fusion candidate (agent I / agent E)
- inputs[1]: could not run — missing: held-out train/test datasets (agent B source cards)
- inputs[2]: could not run — missing: baseline 'naive_resampling' (the thing the claim is against)
- inputs[3]: could not run — missing: at least one baseline named 'single_modality_*'

## Baselines run

_none run_ — no baseline, no claim.

## Preregistered acceptance thresholds

- `min_delta_log_score`: 0.0
- `max_coverage_error`: 0.05
- `max_overconfidence_increase`: 0.02
- `max_delay_rel_error`: 0.15
- `boundary_rel_tol`: 0.05
- `max_hallucination_index`: 1.25
- `min_uncertainty_inflation`: 1.05
- `min_fisher_eig_gain`: 1.1
- `capacity_tol`: 0.1
- `min_model_discrimination`: 0.05
- `n_boot`: 1000

## Explicit non-goals

- This gate does not claim a validated digital twin of any specific person.
- This gate does not claim that any admitted operator is neurally realized.
- No human stimulation protocol is implemented or implied: this is a deep-learning study on open data and there is no device command path.
